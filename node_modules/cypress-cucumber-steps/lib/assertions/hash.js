"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Then_I_see_hash_contains = exports.Then_I_see_hash = void 0;
var cypress_cucumber_preprocessor_1 = require("@badeball/cypress-cucumber-preprocessor");
/**
 * Then I see hash:
 *
 * ```gherkin
 * Then I see hash {string}
 * ```
 *
 * @example
 *
 * ```gherkin
 * Then I see hash "#hash"
 * ```
 *
 * @remarks
 *
 * The URL hash includes the `#` character.
 *
 * @see
 *
 * - {@link Then_I_see_hash_contains | Then I see hash contains}
 */
function Then_I_see_hash(hash) {
    cy.hash().should('equal', hash);
}
exports.Then_I_see_hash = Then_I_see_hash;
(0, cypress_cucumber_preprocessor_1.Then)('I see hash {string}', Then_I_see_hash);
/**
 * Then I see hash contains:
 *
 * ```gherkin
 * Then I see hash contains {string}
 * ```
 *
 * @example
 *
 * ```gherkin
 * Then I see hash contains "hash"
 * ```
 *
 * @see
 *
 * - {@link Then_I_see_hash | Then I see hash}
 */
function Then_I_see_hash_contains(hash) {
    cy.hash().should('contain', hash);
}
exports.Then_I_see_hash_contains = Then_I_see_hash_contains;
(0, cypress_cucumber_preprocessor_1.Then)('I see hash contains {string}', Then_I_see_hash_contains);
//# sourceMappingURL=hash.js.map