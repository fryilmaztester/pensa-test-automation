"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.When_I_find_button_by_text = exports.When_I_find_buttons_by_text = void 0;
var cypress_cucumber_preprocessor_1 = require("@badeball/cypress-cucumber-preprocessor");
var constants_1 = require("../constants");
var utils_1 = require("../utils");
/**
 * When I find buttons by text:
 *
 * ```gherkin
 * When I find buttons by text {string}
 * ```
 *
 * @example
 *
 * ```gherkin
 * When I find buttons by text "Button"
 * ```
 *
 * With [options](https://docs.cypress.io/api/commands/get#Arguments):
 *
 * ```gherkin
 * When I find buttons by text "Button"
 *   | log | true |
 *   | timeout | 4000 |
 *   | withinSubject | null |
 *   | includeShadowDom | false |
 * ```
 *
 * @remarks
 *
 * This precedes steps like {@link When_I_click | "When I click"}. For example:
 *
 * ```gherkin
 * When I find buttons by text "Button"
 *   And I get 1st element
 *   And I click
 * ```
 *
 * @see
 *
 * - {@link When_I_find_button_by_text | When I find button by text}
 */
function When_I_find_buttons_by_text(text, options) {
    (0, utils_1.setCypressElement)((0, utils_1.getButtonElements)(text, constants_1.PseudoSelector.visible, options));
}
exports.When_I_find_buttons_by_text = When_I_find_buttons_by_text;
(0, cypress_cucumber_preprocessor_1.When)('I find buttons by text {string}', When_I_find_buttons_by_text);
/**
 * When I find button by text:
 *
 * ```gherkin
 * When I find button by text {string}
 * ```
 *
 * Finds first button element that matches text.
 *
 * @example
 *
 * ```gherkin
 * When I find button by text "Button"
 * ```
 *
 * With [options](https://docs.cypress.io/api/commands/get#Arguments):
 *
 * ```gherkin
 * When I find button by text "Button"
 *   | log | true |
 *   | timeout | 4000 |
 *   | withinSubject | null |
 *   | includeShadowDom | false |
 * ```
 *
 * @remarks
 *
 * This precedes steps like {@link When_I_click | "When I click"}. For example:
 *
 * ```gherkin
 * When I find button by text "Button"
 *   And I click
 * ```
 *
 * @see
 *
 * - {@link When_I_find_buttons_by_text | When I find buttons by text}
 */
function When_I_find_button_by_text(text, options) {
    (0, utils_1.setCypressElement)((0, utils_1.getButtonElements)(text, constants_1.PseudoSelector.visible, options).first());
}
exports.When_I_find_button_by_text = When_I_find_button_by_text;
(0, cypress_cucumber_preprocessor_1.When)('I find button by text {string}', When_I_find_button_by_text);
//# sourceMappingURL=button.js.map